{
  "healthmonitor": [
    {
      "tenant": "{{ application.tenant }}",
      "state": "{{ application.desired_state }}",
      "name": "{{ application.name }}_healthmonitor",
      "type": "HEALTH_MONITOR_PING",
    }
  ]
}
